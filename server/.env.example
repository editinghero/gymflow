# Recommended (Neon)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require

# Optional (Local Postgres) - only used if DATABASE_URL is not set
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gym_management
DB_USER=postgres
DB_PASSWORD=your_password

JWT_SECRET=replace_me
PORT=5000
